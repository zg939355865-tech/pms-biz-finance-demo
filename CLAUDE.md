# PMS 业财一体化系统

## 项目概述
- **项目名称**: PMS 业财一体化系统
- **项目性质**: 持久性项目，需求分次交付
- **交付形式**: 高保真原型 + 静态页面（支持上传至 devfile.cn / 艾可秀等平台做在线展示）
- **原型技术栈**: Vue3 (CDN) + Bootstrap Table + Tailwind
- **使用场景**: 公司内部管理系统 / 上传至在线平台做静态展示

## 规范引用
项目开发必须严格遵守以下规则文件：
- `rules/common.md` — 项目角色定义（可复用到其他项目）
- `rules/ui-standard.md` — UI设计标准
- `rules/interaction.md` — 交互规范
- `rules/data-standard.md` — 数据标准
- `rules/page-blueprint-standard.md` — 自然语言需求生成页面蓝图 MD 的规范
- `rules/resource-center-standard.md` — 页面必须引用原型资源中心的防漂移规范
- `rules/project-standard.md` — 项目级页面、列表、状态、权限和交付准则（优先级最高）
- `prd-assets/prd-document-standard.md` — PRD/UR模板、内容结构、生成流程和验收规范

## 开发要求
1. 每次回答问题，必须详细展示思考过程
2. 接到任务后说：接收任务
3. 完成任务后说：完成任务
4. 任务完成后，告知生成的内容在什么位置即可，不需要展示内容
5. **每次生成 UI 页面时，必须调用 `ui-ux-pro-max` skill**
6. 所有开发工作必须遵守 `rules/` 下的各项规范
7. 新增规则时会追加到对应规则文件中
8. **直接执行原则**: 接到任务后立即执行，不主动询问确认。如需用户提供信息时再询问，其余情况直接干活**
9. **PRD/UR生成**: 默认使用 `$mes-ur-generator`，完整遵循该 Skill 自带的文档结构、格式和生成流程，并执行 `prd-assets/prd-document-standard.md` 中的内容与验收要求；生成后必须全页渲染并逐页检查。
10. **增量变更防回退**: 已有页面只修改用户明确指定区域；单页局部需求禁止运行模块级批量生成脚本。修改前读取蓝图、Schema、HTML、专项检查作为基线，专项检查只追加本次断言、不得删除原断言；生成后核对非本次字段和操作保持不变。
11. **检查范围分级**: 单页局部需求只执行目标页面的蓝图、Schema、HTML、专项交互回归及 1440px/2048px 页面检查，不运行组件覆盖、资源中心、全页面 Harness 或其他项目级检查。仅当本次实际修改公共组件、公共样式、公共运行时、公共策略、公共生成器逻辑、资源中心或菜单框架时，才执行受影响的项目级检查；不得以"稳妥"为由扩大检查范围。
12. **回复简洁**: 执行任务时只输出结论、变更文件路径和关键变更点，不展示中间工具调用过程（如 Edit/Grep/Read 等操作详情）。用户主动要求查看细节时再展开。

## 执行效率规则（硬约束）
1. **禁止全读 HTML**: HTML 是构建产物，单文件 200KB+。需要检查时用 `grep` 定位片段或 `read` 指定 offset/limit 读取局部，禁止 `read` 整个 HTML 文件。
2. **规则按需加载**: 8 个 rules 文件共 38KB，但大部分规则已编码到策略引擎和检查器。按任务类型加载：
   - 新页面生成：必读 `project-standard.md`，按需读 `ui-standard.md`（仅布局节）
   - 加字段/改文案：只读 `project-standard.md` §6-7（列表和详情规范）
   - 修复视觉问题：只读 `ui-standard.md`
   - 修改蓝图：只读 `page-blueprint-standard.md`
   - 其余情况不读 rules 全文
3. **轻微变更快速通道**: 只改 Schema 的 fields/columns/mockData 且不新增组件时，跳过蓝图环节，直接 Schema patch → `npm run verify:page` → 交付。
4. **验证聚合命令**: 单页验证统一使用 `npm run verify:page -- <schema-path>`，一条命令完成构建 + 策略检查 + 静态检查 + 视口检查。禁止手动分步调用 `prototype:build`、`check:all-pages` 等多个命令。

## 规范检查清单（每次交付前必检）
- [ ] 颜色使用正确（主色/次色/状态色）
- [ ] 间距符合8px系统
- [ ] 圆角统一（按钮6px/卡片8px/模态框12px）
- [ ] 阴影层级正确
- [ ] 字体层级正确（标题20px/正文14px/辅助12px）
- [ ] 字段对齐正确（编码左/日期左/数值右/状态中）
- [ ] 状态标签颜色统一（6种状态）
- [ ] 按钮样式一致（主/次/危险/文字/禁用）
- [ ] 表格样式统一（表头背景/行高/斑马纹）
- [ ] 日期格式统一（YYYY-MM-DD HH:mm:ss）
- [ ] 业财数据一致性（业务单据金额 = 财务凭证金额）
- [ ] 业务-财务穿透链路完整（项目→合同→预算→收支→凭证）

## 已确定的框架规范
1. **页面布局**: 经典后台管理系统布局（左侧固定菜单栏 + 顶部工具栏 + 右侧主内容区）
2. **菜单模式**: 侧边菜单 + 顶部标签页（支持多标签并行切换）
3. **颜色主题**: 深蓝科技风（专业稳重，B端企业管理风格）
4. **字体规范**: 阿里巴巴普惠体 / 钉钉系列（现代简洁，互联网风格）
5. **图标规范**: 阿里巴巴图标库 (iconfont)（与字体同源，保持视觉一致性）
6. **命名规范**: JS用camelCase，CSS用kebab-case，Vue组件用PascalCase
7. **交付方式**: 按菜单模块分次交付（用户每次给1个或多个模块需求，完成后交付）
8. **更新机制**: 覆盖交付（每次交付完整HTML文件，替换旧版本即可）
9. **页面路径**: 所有页面统一存放在 `pages/` 目录下

## 前端架构
### iframe + 共享菜单架构
- `index.html` — 主框架（含菜单、标签页、iframe内容区）
- 功能页面 — 仅内容区，无菜单（通过 iframe 嵌入）
- 分享方式 — 分享 `index.html` 整体使用，无需单独分享功能页

### 菜单结构
| 一级菜单 | 二级菜单 | 说明 |
|---------|---------|------|
| 流程指引 | 无 | 展示流程图，无子页面 |
| 待办事项 | 我的审批、我的任务 | |
| 项目前期 | 项目需求、调研问卷、项目方案、项目实施通知 | |
| 项目管理 | 项目类型、计划模板、项目立项、项目计划、项目进度填报 | |
| 报表管理 | WBS任务看板、智能体运营分析、智能体运行监控、需求跟踪表 | |
| 使用指南 | 无 | |

### 标签页机制
- 点击菜单 → 检查是否已开标签 → 无则新增，有则切换
- 点击标签 → 切换 iframe src
- 点击 × → 关闭标签（至少保留1个）
- 子页面返回 → 通过 postMessage 通知父窗口打开指定标签

## PMS业财一体化核心架构
1. **项目为核心**: 项目是业务与财务的共同载体，项目数据贯穿始终
2. **业务驱动财务**: 业务单据（合同/开票/收付款）自动生成财务凭证
3. **预算前置控制**: 业务发生时实时检查预算余额，超预算可配置拦截或审批
4. **多核算主体**: 支持多公司/多部门独立核算与汇总分析
5. **实时业财对账**: 业务单据与财务凭证一一对应，差异可追溯

## 开发流程
```
【新页面】需求 → 头脑风暴 → Schema → 摘要确认 → verify:page → 交付
【轻微变更】需求 → Schema patch → verify:page → 交付
```

### 新页面流程
1. 【接收需求】用户提出模块需求
2. 【头脑风暴】brainstorming skill 讨论需求（明确业务场景、业财数据流向）
3. 【Schema 编写】直接生成正式 Schema（参考 `schemas/SCHEMA_GUIDE.md`，不写策略引擎自动补齐的字段）
4. 【摘要确认】运行 `npm run schema:summary -- <schema-path>` 生成人类可读摘要，用户确认后继续
5. 【验证交付】运行 `npm run verify:page -- <schema-path>`，通过后交付

### 轻微变更流程（加字段/改文案/改 mockData）
1. 【接收需求】用户指定变更内容
2. 【Schema patch】直接修改 Schema 中的 fields/columns/mockData
3. 【验证交付】运行 `npm run verify:page -- <schema-path>`，通过后交付

### 关键命令
- `npm run verify:page -- <schema-path>` — 一条命令完成构建 + 策略检查 + 静态检查 + 视口检查
- `npm run schema:summary -- <schema-path>` — 从 Schema 生成人类可读摘要
- Schema 编写指南见 `schemas/SCHEMA_GUIDE.md`
