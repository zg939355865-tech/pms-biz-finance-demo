# 交付物资源

本目录统一声明项目可生成的交付物类型。

- HTML 原型：已接入 Harness，资源中心为 `prototype-resources/`。
- PRD/解决方案文档：保留 `document` 通道，后续有稳定模板时再接入机器规则。
- 解决方案 PPT：保留 `presentation` 通道，后续有稳定母版时再接入机器规则。

未接入检查器的通道不得标记为自动验收完成。
