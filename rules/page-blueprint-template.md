# `{页面名称}`页面蓝图

## 1. 蓝图信息

| 项目 | 内容 |
| --- | --- |
| 蓝图状态 | 待确认 / 已确认 / 需修改 / 已变更 |
| 页面名称 | `{中文页面名称}` |
| 页面编码 | `{英文页面编码}` |
| 业务域 | `{中文业务域}（{domain}）` |
| 菜单路径 | `{一级菜单} / {二级菜单}` |
| 页面类型 | `{列表详情一体、流程审批单据、非流程录入、看板、配置等}` |
| 生成范围 | `{页面包包含的视图}` |
| 共用范围 | `{哪些视图共用一个 HTML}` |
| 计划输出 | `pages/{domain}/{page-code}.html` |
| 参考依据 | `{需求、截图、现有页面、业务资料、资源中心}` |

## 2. 需求理解与 AI 假设

### 2.1 用户明确要求

1. `{用户已经明确的要求}`

### 2.2 AI 补充内容与假设

1. `{依据项目规范补充的页面、字段、操作、数据及交互}`
2. `{未经用户明确确认的内容必须标记为“AI 假设”}`

## 3. 业务目标与范围

### 3.1 业务目标

`{页面解决的业务问题和业务价值}`

### 3.2 主要使用人员

- `{角色：使用场景和职责}`

### 3.3 本次包含

- `{本期功能}`

### 3.4 本次不包含

- `{明确排除的功能；没有则写“不适用”}`

### 3.5 业务链路

```text
{上游} -> {当前单据} -> {下游} -> {财务结果}
```

## 4. 页面组成与关系

| 视图 | 载体 | 说明 |
| --- | --- | --- |
| `{列表/新增/编辑/查看/审核/弹窗}` | `{同一 HTML / 弹窗 / 独立页面}` | `{用途}` |

页面关系：

- `{入口、跳转、返回、共用 HTML 和穿透关系}`

## 5. 页面区域

### 5.1 列表视图

1. `{区域名称：用途、组件、默认状态、是否可折叠}`

### 5.2 详情视图

1. `{区域名称：用途、组件、默认状态、是否可折叠}`

不适用的视图必须保留小节并写“不适用”。

## 6. 字段定义

查询、列表、主表、明细、附件及其他字段按实际页面分别保留小节；不存在的字段类型写“不适用”。

仅明细字段：`{填写只属于明细、不允许出现在整单查询条件和主列表中的字段；没有则写“不适用”}`。正式 Schema 必须将这些字段编码写入 `detailOnlyFields`。

### 6.1 查询字段

| 字段 | 控件 | 默认展示 | 数据来源 | 规则或说明 |
| --- | --- | --- | --- | --- |
| `{字段}` | `{控件}` | 是/否 | `{来源}` | `{规则}` |

### 6.2 列表字段

| 顺序 | 字段 | 展示类型 | 对齐 | 规则或说明 |
| ---: | --- | --- | --- | --- |
| 1 | `{字段}` | `{类型}` | 左/中/右 | `{规则}` |

### 6.3 基础信息字段

| 字段 | 控件 | 必填 | 只读 | 数据来源 | 规则或说明 |
| --- | --- | --- | --- | --- | --- |
| `{字段}` | `{控件}` | 是/否 | 是/否 | `{来源}` | `{规则}` |

### 6.4 明细字段

| 顺序 | 字段 | 控件 | 必填 | 只读 | 规则或说明 |
| ---: | --- | --- | --- | --- | --- |
| 1 | `{字段}` | `{控件}` | 是/否 | 是/否 | `{规则}` |

### 6.5 附件或其他字段

| 字段 | 控件或展示类型 | 必填 | 只读 | 数据来源 | 规则或说明 |
| --- | --- | --- | --- | --- | --- |
| `{字段}` | `{控件}` | 是/否 | 是/否 | `{来源}` | `{规则}` |

## 7. 操作定义

### 7.1 列表操作

| 操作 | 位置 | 可用条件 | 执行结果 | 是否确认 |
| --- | --- | --- | --- | --- |
| `{操作}` | `{位置}` | `{条件}` | `{结果}` | 是/否 |

### 7.2 详情操作

| 操作 | 位置 | 可用条件 | 执行结果 | 是否确认 |
| --- | --- | --- | --- | --- |
| `{操作}` | `{位置}` | `{条件}` | `{结果}` | 是/否 |

## 8. 状态与权限

有状态流转时：

| 状态 | 页面只读性 | 可执行操作 | 可编辑内容 | 下一状态 |
| --- | --- | --- | --- | --- |
| `{状态}` | `{只读/可编辑}` | `{操作}` | `{范围}` | `{下一状态}` |

无状态流转时必须明确写“本页面无审批状态流转”，并提供权限矩阵。无流程录入类页面的详情主操作统一为“提交”，提交仅保存业务数据并返回列表，不发起审批流程，不得使用“保存”或“保存草稿”：

| 权限 | 列表 | 详情 | 可执行操作 |
| --- | --- | --- | --- |
| `{权限}` | `{列表能力}` | `{详情能力}` | `{操作}` |

## 9. 业务规则与校验

| 编号 | 触发时机 | 条件 | 处理结果 | 提示文案 |
| --- | --- | --- | --- | --- |
| R01 | `{时机}` | `{可验证条件}` | `{结果}` | `{明确文案/不适用}` |

必须覆盖必填、格式、范围、条件显示、主明细平衡、状态限制、删除、引用关系及业财金额一致性；不适用的规则类型写明“不适用”。

## 10. 关键交互

1. `{查询、重置和展开筛选}`
2. `{分页、固定列、滚动和列设置}`
3. `{新增、编辑、查看和返回}`
4. `{页签、弹窗、数据选择和实时计算}`
5. `{加载、空数据、失败、禁用和未保存离开}`
6. `{iframe 与菜单标签通信}`

## 11. 业财关联

### 11.1 归集维度

- `{公司、核算主体、项目、部门、客户或供应商}`

### 11.2 上下游关系

- 上游来源：`{来源}`
- 当前单据：`{当前对象}`
- 下游单据：`{下游}`
- 穿透链路：`{链路}`

### 11.3 财务影响

`{预算、应收应付、收付款、发票、凭证和金额一致性；没有直接影响时明确说明}`

## 12. 组件与样例映射

资源中心基线：`pms-resource-center-v1`。

| 页面区域 | Schema 组件 | 参考结构 |
| --- | --- | --- |
| `{区域}` | `{已注册组件}` | `{资源中心样例路径}` |

参考页面样例：

- `{样例名称及使用范围}`

必要差异：`{与样例的差异；没有则写“无”}`。

## 13. 样例数据

1. 默认准备 12 条高质量业务样例数据。
2. `{覆盖状态、类型、边界场景、金额、日期、编码和关联关系}`
3. 列表固定每页 5 条，不提供页容量选择，不产生业务区纵向滚动条。

## 14. 验收标准

- [ ] `{可观察、可操作的验收项}`
- [ ] 页面可以直接打开并支持 iframe 嵌入。
- [ ] 列表固定每页 5 条，不提供页容量选择，不产生业务区纵向滚动条。
- [ ] Schema、页面结构、组件覆盖及视觉检查通过后才交付 HTML。

## 15. 待确认项

- 无；或列出尚未确认的问题编号及具体问题。

## 16. 确认记录

| 日期 | 操作人 | 结论 | 说明 |
| --- | --- | --- | --- |
| YYYY-MM-DD | AI | 待确认 | 已生成蓝图，暂不生成 Schema 和 HTML |
| YYYY-MM-DD | 用户 | 已确认 | 可以生成正式 Schema 和 HTML |
