# 采购管理新增按钮文案优化

## 变更范围

- 采购管理下所有已实现业务列表页的主新增按钮统一显示“新增”。
- 同步 Schema、页面蓝图、生成 HTML 和回归断言。
- 保持按钮位置、主色、加号图标、点击目标、权限、状态及新增详情逻辑不变。
- 页面标题、菜单名称、详情标题及正文中的业务描述保持原业务名称。

## 验收标准

- 采购申请、支出合同、采购订单、采购入库、付款申请、采购收票、付款单、票款核销列表页的新增按钮均显示“新增”。
- 按钮的 `aria-label` 与 `title` 同步为“新增”。
- 固资入库仍为未实现菜单占位，不新增页面或按钮。
