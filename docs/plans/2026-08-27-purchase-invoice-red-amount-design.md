# 采购收票红字发票金额设计

## 设计结论

- 发票金额由类型控制符号：红字为负数，蓝字为正数。
- 红字状态输入正数时自动转换为对应负数；输入负数时保持不变。
- 类型切换时同步转换已有发票金额，避免类型与金额符号不一致。
- 金额仍使用数值输入并保留两位小数，不增加提示文案或额外操作。

## 实现与验证

- Schema 通过公共 `signByFieldValue` 契约声明类型与金额符号关系。
- 页面生成器统一处理输入、类型切换和已有数据回填场景。
- 专项回归验证红字正数转负数、负数保持，以及切回蓝字恢复正数。
