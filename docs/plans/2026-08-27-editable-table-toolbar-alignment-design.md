# 可编辑明细表工具栏对齐设计

## 设计结论

- 修复范围为公共 `EditableTable`，不增加采购收票页面私有样式。
- 区块标题与工具栏首个按钮使用相同左内边距，移除空占位元素造成的额外横向间距。
- 工具栏使用垂直居中布局，按钮中心线与工具栏中心线一致。
- 同一详情页的多张明细表继承同一规则，空数据、有数据及不同桌面视口保持一致。

## 验证

- 采购收票专项回归对“订单明细”和“收票结算明细”分别执行像素级左边界与垂直中心断言。
- 公共组件资源检查和全页面桌面视口检查验证无回归。
